# I2C-Protocol
Verilog project implements a synchronous I2C Protocol Controller for two-wire serial communication using SDA and SCL lines. The modular architecture features a clock generator, bit counter, and an FSM managing START/STOP states, byte transfers, and ACK/NACK validation. It handles multi-master arbitration and ensures glitch-free data transmission.
